3.3.5.2 การพัฒนาฟังก์ชันการทำงานส่วนหน้าจอหลัก (Main Functions Development)
เพื่อให้ส่วนต่อประสานกับผู้ใช้งานสามารถทำงานร่วมกับฮาร์ดแวร์เซนเซอร์และการประมวลผลข้อมูลทางชีวกลศาสตร์ได้อย่างสมบูรณ์ คณะผู้จัดทำได้ดำเนินการเขียนชุดคำสั่ง (Source Code) ใน Flutter Framework สำหรับแผงควบคุมหลัก โดยอธิบายตามโครงสร้างการทำงานได้ดังนี้
ก) หน้าต้อนรับ (Splash Screen) พัฒนาในไฟล์ splash_screen.dart หน้าจอใช้ AnimationController ร่วมกับ CurvedAnimation สร้างเอฟเฟกต์การค่อย ๆ ปรากฏขึ้นอย่างนุ่มนวลภายในระยะเวลา 1,500 มิลลิวินาที ขณะเดียวกันฟังก์ชัน _checkAuth() จะทำงานคู่ขนานโดยรอ 2,000 มิลลิวินาทีให้แอนิเมชันเล่นจบก่อน แล้วจึงตรวจสอบสถานะ Session Token ของผู้ใช้งานผ่านคลาวด์ หากพบว่ายังมี Session ที่ใช้งานได้อยู่ ระบบจะนำผู้ใช้งานตรงไปยังหน้าหลักอัตโนมัติ 
ข) หน้าเข้าสู่ระบบและหน้าสมัครสมาชิก (Auth Screen) พัฒนาร่วมกันในไฟล์ auth_screen.dart โดยใช้ตัวแปร _isLogin เป็นตัวสลับโหมดระหว่างฟอร์มเข้าสู่ระบบกับฟอร์มสมัครสมาชิก เมื่อยืนยันตัวตนสำเร็จ ระบบจะทำงานร่วมกับ Supabase SDK เพื่อสร้าง Session Token อัตโนมัติ พร้อมอัปเดตและเรียกใช้งานข้อมูลโปรไฟล์ผ่านคำสั่ง UPSERT ลงในตาราง profiles ของฐานข้อมูล
ค) หน้าแผงควบคุมหลัก (Dashboard Screen) พัฒนาในไฟล์ dashboard_screen.dart เป็นหน้าจอหัวใจสำคัญ ใช้พื้นที่แสดงภาพวิดีโอสดจากกล้องโดยรับค่าจาก CameraPreview Widget ควบคู่กับป้ายสถานะบันทึก (REC) ถัดลงมาเป็นกราฟเส้นแสดงแรงกดเรียลไทม์ที่วาดผ่านฟังก์ชัน LineChart ให้เลื่อนไปทางซ้ายตามเวลาจริง ไฮไลท์หลักคือการเรนเดอร์แผนภาพความร้อนแบบนุ่มนวล (Smooth Heatmap) ที่ใช้ Bilinear Interpolation ร่วมกับการคำนวณและวาดตำแหน่ง Center of Pressure (CoP) แบบเรียลไทม์ตามจังหวะวิดีโอ

```dart
// ตัวอย่างชุดคำสั่งการเรนเดอร์แจกแจงโครงสร้างหน้าแผงควบคุม (Dashboard)
Expanded(
  child: Row(
    children: [
      // คอลัมน์ซ้าย: วิดีโอกล้องสด และ กราฟเส้น
      Expanded(
        child: Column(
          children: [
            CameraPreview(_cameraController),
            RealtimeLineChart(dataStream: _forceDataStream),
          ],
        ),
      ),
      // คอลัมน์ขวา: สมดุลซ้าย-ขวา และ แผนภาพความร้อน
      Expanded(
        child: Column(
          children: [
            BalanceBarWidget(left: _leftForce, right: _rightForce),
            CustomPaint(painter: CombinedHeatmapPainter(_heatmapGrid)),
          ],
        ),
      ),
    ],
  ),
)
```
ภาพที่  3-27 ภาพตัวอย่างชุดคำสั่งโครงสร้างหน้าแผงควบคุมหลัก (Dashboard Layout)
จากภาพที่ 3-27 แสดงโค้ดการแบ่งโครงสร้างหน้าจอด้วย Widget `Row` และ `Expanded` เพื่อแยกพื้นที่การประมวลผลภาพวิดีโอมอนิเตอร์และกราฟแรงกดไว้ทางซ้าย และแสดงแผนภาพความร้อน `CombinedHeatmapPainter` ไว้ทางขวา ส่งผลให้การแสดงผลแยกส่วนทำงานได้อย่างอิสระและเป็นระเบียบ
ง) หน้าเชื่อมต่อฮาร์ดแวร์ (Sensor Connection UI) พัฒนาในไฟล์ sensor_display_screen.dart มีการสร้าง Animation แบบ Pulse (เรืองแสงกระพริบ) ตามสถานะการค้นหา เมื่อเลือกรับพอร์ตอนุกรม (COM Port) ซอฟต์แวร์จะรับข้อมูลสตรีมมิงผ่านไลบรารี flutter_libserialport และแสดงอัตราจำนวนแพ็กเกจ (Packets) ที่เครื่องรับได้ต่อมิลลิวินาที
จ) หน้าประวัติการฝึกซ้อม (History Screen) พัฒนาในไฟล์ history_screen.dart สร้างขึ้นเพื่อดึงข้อมูลจากตาราง swings ด้วยฟิลเตอร์ user_id ของผู้ใช้ปัจจุบันแบบ Real-time Stream ข้อมูลถูกนำมาตรวจสอบความปลอดภัยด้วย Row Level Security (RLS) และวาดรายการผลลัพธ์ผ่าน List View โดยมีฟังก์ชัน Swipe-to-Delete เป็นตัวสั่งการลบเอกสารออกจากคลาวด์ 

```dart
// ตัวอย่างชุดคำสั่งการเรียกดูข้อมูลจาก Supabase แบบ Real-time Stream
StreamBuilder(
  stream: Supabase.instance.client
      .from('swings')
      .stream(primaryKey: ['id'])
      .eq('user_id', Supabase.instance.client.auth.currentUser!.id)
      .order('created_at', ascending: false),
  builder: (context, snapshot) {
    if (snapshot.connectionState == ConnectionState.waiting) {
      return const CircularProgressIndicator();
    }
    final swingsData = snapshot.data as List<Map<String, dynamic>>;
    return ListView.builder(
      itemCount: swingsData.length,
      itemBuilder: (context, index) {
        return HistoryCardWidget(swing: swingsData[index]);
      },
    );
  },
)
```
ภาพที่  3-28 ภาพตัวอย่างชุดคำสั่งระบบประวัติการฝึกซ้อม (Cloud Syncing)
จากภาพที่ 3-28 แสดงโค้ดการใช้งานกรอบโครงสร้าง `StreamBuilder` ร่วมกับ Supabase SDK ของตาราง `swings` โดยได้นำเมธอด `.eq('user_id')` มาประยุกต์ใช้เพื่อกรองเฉพาะชุดข้อมูลของผู้ใช้งานที่กำลังเข้าสู่ระบบและพิสูจน์ตัวตน (Authentication) ทำให้หน้าประวัติมีความปลอดภัยและเป็นส่วนบุคคล
ฉ) หน้าเล่นซ้ำวงสวิง (Playback Screen) พัฒนาในไฟล์ playback_screen.dart ระบบดึงข้อมูลโครงสร้าง JSON Array ทั้งชุด Timestamp วิดีโอและเซนเซอร์มาเรียงลงหน่วยความจำ รูปแบบแผนภาพความร้อนวิดีโอใช้เทคนิค Pre-compute Frames เพื่อลดภาระการคำนวณซ้ำซ้อน กลไกการเชื่อมเวลาถูกควบคุมอย่างละเอียดด้วย Timer.periodic ซึ่งสอดคล้องกับตำแหน่งปัจจุบันของการเล่น (videoController.value.position) ในระยะรอบทุกๆ 16 มิลลิวินาที ส่งผลให้บริการแสดงผลตอบสนองโดยไม่มีอาการภาพหน่วง

3.3.5.3 การติดตั้งและกำหนดค่าสภาพแวดล้อม Flutter
ดาวน์โหลดและติดตั้ง Flutter SDK จากเว็บไซต์ทางการ flutter.dev จากนั้นกำหนดค่า Path Variable ให้ระบบปฏิบัติการรู้จักคำสั่ง flutter และ dart ติดตั้ง Dart Extension และ Flutter Extension บน Visual Studio Code เพื่อเปิดใช้งาน IntelliSense, Hot Reload และ Emulator Manager แล้วรันคำสั่ง flutter doctor เพื่อตรวจสอบว่าสภาพแวดล้อมพร้อมใช้งานครบถ้วน

ก)	โครงสร้างซอฟต์แวร์และการจัดการ Dependenciesสร้างโปรเจกต์ Flutter ใหม่ด้วยคำสั่ง flutter create แล้วจัดโครงสร้างโฟลเดอร์ตามหลัก Service-Oriented Architecture โดยแบ่งเป็นโฟลเดอร์ screens สำหรับหน้าจอแสดงผลต่าง ๆ โฟลเดอร์ services สำหรับ SerialService และ SensorDataService โฟลเดอร์ widgets สำหรับ Reusable Widget และโฟลเดอร์ models สำหรับโครงสร้างข้อมูล จากนั้นเพิ่ม Package ที่จำเป็นในไฟล์ pubspec.yaml ได้แก่ supabase_flutter สำหรับเชื่อมต่อ Supabase, flutter_libserialport สำหรับรับข้อมูลจาก Serial Port, camera สำหรับบันทึกวิดีโอ, fl_chart สำหรับแสดงผลกราฟสถิติ, video_player สำหรับเล่นวิดีโอในหน้า Playback และ flutter_dotenv สำหรับจัดการตัวแปรสภาพแวดล้อม แล้วรันคำสั่ง flutter pub get เพื่อดาวน์โหลด Package ทั้งหมด
ข)	ปัญหาและการจัดการอุปกรณ์กล้องเสริม (Webcam Integration) ในส่วนของการเชื่อมต่อกล้องวิดีโอเสริม พบปัญหาว่าไลบรารีกล้องบนระบบปฏิบัติการ Windows มักจะคืนค่าชื่ออุปกรณ์ (Device Path) ที่ซับซ้อนและยาวเกินไป เช่น มีรหัส USB VID/PID ปะปนอยู่ด้วย ทำให้ผู้ใช้งานไม่สามารถแยกแยะและเลือกกล้องจาก Dropdown Menu ได้อย่างรวดเร็ว ผู้วิจัยจึงแก้ไขด้วยการเพิ่มฟังก์ชันคัดกรองชื่อ (String Parsing) โดยใช้คำสั่ง split('<').first.trim() เพื่อตัดอักขระพิเศษและ Device Path ส่วนเกินออก แล้วสกัดเอาเฉพาะชื่อที่เป็นมิตรกับผู้ใช้ (User-friendly Name) เช่น Webcam หรือ Integrated Camera มาแสดงผลใน Dropdown Menu ซึ่งช่วยเพิ่มประสบการณ์การใช้งาน (UX) ให้ดีขึ้น
3.3.5.4 การสถาปัตยกรรมการรับข้อมูลและการถอดรหัส (Data Parsing and Mapping)
สร้างคลาส SerialService แบบ Singleton Pattern เพื่อควบคุมสถานะการเปิดพอร์ต และรับข้อมูลแบบสตรีมต่อเนื่องผ่านflutter_libserialport โดยใช้ StreamController เพื่อส่งข้อมูลไปยังทุกหน้าจอที่ Subscribe อยู่พร้อมกัน
ก)	การประมวลผล Buffer และ Parsing ข้อมูล CSV เขียนฟังก์ชัน _processData() ภายใน SerialService ให้รับข้อมูลไบต์อาร์เรย์ (Uint8List) แปลงเป็น String สะสมใน _buffer จนตรวจพบ \n จึงตัดแยกเป็นก้อนข้อมูลหนึ่งเฟรม แล้ว Tokenize ด้วย split(',') ให้ได้ Array ของตัวเลข Integer จำนวน 64 ค่าต่อหนึ่งเฟรม วิธีนี้ช่วยป้องกันปัญหาแพ็กเกจข้อมูลที่ขาดหายจากการสตรีมต่อเนื่อง

```dart
// ตัวอย่างชุดคำสั่งการจัดระเบียบข้อมูลดิบตามบรรทัด (Raw Data Parsing)
void _processData(Uint8List data) {
  _buffer += utf8.decode(data);
  // ลูปรับข้อมูลจนกว่าจะตรวจพบสัญลักษณ์สิ้นสุดบรรทัด (\n)
  while (_buffer.contains('\n')) {
    int newlineIndex = _buffer.indexOf('\n');
    String frame = _buffer.substring(0, newlineIndex).trim();
    _buffer = _buffer.substring(newlineIndex + 1);

    // แยกชุดตัวเลขแพ็กเกจด้วยเครื่องหมายจุลภาค
    List<String> values = frame.split(',');
    if (values.length == 64) {
      List<int> parsedData = values.map((e) => int.parse(e)).toList();
      _streamController.add(parsedData); // พ่นเข้า Stream
    }
  }
}
```
ภาพที่  3-29 ภาพตัวอย่างชุดคำสั่งแยกส่วนข้อมูลที่ได้รับจาก Serial Port
จากภาพที่ 3-29 แสดงโค้ดการแปลงบล็อกข้อมูลไบต์ (Uint8List Array) ที่ได้รับจากชิปมัลติเพล็กเซอร์มาเรียงในรูปแบบข้อมูลสตริง (_buffer) ก่อนที่จะดำเนินการหาจุดสิ้นสุดบรรทัดเพื่อป้องกันบักเฟรมตกหล่น (Missing Packets) และแปลค่าจากนั้นลงช่องตัวแปรให้พร้อมสำหรับการนำไปคำนวณกราฟิก
ข)	การกรอง Threshold และการแมปข้อมูลเป็น Grid 5x5 ระบบประเมินแรงกดรวม (Total Force) ก่อนการพล็อตกราฟ หากน้ำหนักรวมไม่ถึงค่า _forceThreshold ที่กำหนดไว้ เช่น 300 แอปพลิเคชันจะถือว่าเป็น Noise หรือไม่มีผู้ใช้งานยืนอยู่ และแสดงค่าเป็น 0% เพื่อป้องกันข้อผิดพลาด Divide-by-Zero จากนั้นเขียนฟังก์ชัน _mapTo5x5Grid() เพื่อแยกข้อมูล Array ออกเป็นเซตเท้าซ้าย (Index 32 ถึง 56) และเซตเท้าขวา (Index 0 ถึง 24) แล้วจัดเรียงเป็น 2D Array ขนาด 5x5 อย่างไรก็ตาม ในกระบวนการนี้พบปัญหาตำแหน่งเซนเซอร์สลับทิศทาง (Heatmap Orientation) ทั้งการกลับหัวบน-ล่างและการสลับซ้าย-ขวาของเท้าขวา ทำให้ภาพกราฟิกไม่ตรงกับตำแหน่งกายวิภาคจริงของเท้า ผู้วิจัยได้แก้ไขโดยการปรับแก้อัลกอริทึมแมปปิ้งให้ทำการกลับพิกัด (Reverse List) ในแต่ละแถวของเท้าขวา และนำสมการพลิกแกน Y ออกจากส่วน CustomPainter เพื่อให้การเรนเดอร์ภาพตรงกับตำแหน่งกายวิภาคของเซนเซอร์บนพื้นจริง
3.3.5.5 กลไกการเรนเดอร์แผนภาพความร้อน (Advanced Heatmap Rendering)
        เนื่องจากการพล็อต Widget ทีละเม็ดใน Flutter อาจทำให้เกิดความหน่วงในการเรนเดอร์ ระบบจึงออกแบบให้ใช้ CustomPainter เข้าถึงระดับ Canvas โดยตรงเพื่อวาดภาพ Heatmap ของเท้าทั้งสองข้างในคราวเดียวกัน
ก)	การพัฒนา CombinedHeatmapPainter โดยการสร้างคลาสCombinedHeatmapPainter ในไฟล์ combined_heatmap.dart ซึ่งสืบทอดจาก CustomPainter เพื่อวาด Color Matrix ของเท้าซ้ายและขวาบน Canvas โดยตรง ทำให้สามารถควบคุมการเรนเดอร์ได้อย่างละเอียดและมีประสิทธิภาพสูง โดยวาดเมทริกซ์สีพื้นที่ตารางเท้าทั้งสองข้างลงบนเลเยอร์ลึกสุดในคราวเดียว
ข)	อัลกอริทึม Bilinear Interpolation เนื่องจากเซนเซอร์ฮาร์ดแวร์มีความละเอียดจำกัดเพียง 5x5 Grid ระบบจึงประยุกต์ใช้ฟังก์ชัน _bilinearInterpolate() เพื่อคำนวณประมาณค่าพิกเซลที่อยู่ระหว่างจุดเซนเซอร์จริงโดยคำนวณจากน้ำหนักของเซนเซอร์โดยรอบ 4 จุด ทำให้ภาพ Heatmap ที่แสดงผลบนหน้าจอมีความต่อเนื่องและนุ่มนวล (Smooth Continuous Surface) เทียบเท่าภาพความละเอียด 40x60 พิกเซล

```dart
// ตัวอย่างสมการคำนวณปรับเรียบแผนภาพ (Bilinear Interpolation Equation)
double _bilinearInterpolate(double x, double y, List<List<double>> grid) {
  int x1 = x.floor();
  int x2 = x1 + 1 < grid[0].length ? x1 + 1 : x1;
  int y1 = y.floor();
  int y2 = y1 + 1 < grid.length ? y1 + 1 : y1;

  double dx = x - x1;
  double dy = y - y1;

  double p1 = grid[y1][x1];
  double p2 = grid[y1][x2];
  double p3 = grid[y2][x1];
  double p4 = grid[y2][x2];

  // คืนค่าเฉลี่ยถ่วงน้ำหนักโดยอ้างอิงกับพิกัดระยะกระจัดของ 4 จุดรอบมุม
  return p1 * (1 - dx) * (1 - dy) +
         p2 * dx * (1 - dy) +
         p3 * (1 - dx) * dy +
         p4 * dx * dy;
}
```
ภาพที่  3-30 ภาพตัวอย่างชุดคำสั่งประมวลผลแผนภาพความร้อน (Bilinear Operation)
จากภาพที่ 3-30 แสดงโค้ดการคำนวณสร้างชุดพิกเซลจำลองขึ้นมาแทรกกลางระหว่างจุดสัมผัสเซนเซอร์ตามพิกัดของฮาร์ดแวร์จริง (P1 ถึง P4) การทบน้ำหนักการเหลื่อมล้ำของพิกัดแนวแกน X (dx) และแนวแกน Y (dy) นี้ส่งผลมหาศาลให้คุณภาพตารางเมทริกซ์ 5x5 มีความละเอียดของความหนาแน่นสีสูงขึ้นจนดูพรีเมียมปราศจากร่องขอบเหลี่ยม
ค)	การทำ Color Spectrum Mapping เขียนฟังก์ชัน _getHeatColor() เพื่อแปลงค่าแรงกดสัมพัทธ์ในช่วง 0.0 ถึง 1.0 ให้กลายเป็น Gradient สีแบบ Thermal Spectrum โดยสีน้ำเงินแทนแรงกดต่ำ ไล่ผ่านสีเขียว เหลือง ส้ม จนถึงสีแดงที่แทนแรงกดสูงสุด ณ ตำแหน่งที่เท้ากระทบพื้นมากที่สุด
ง)	การติดตาม Center of Pressure แบบเรียลไทม์ คำนวณตำแหน่ง Center of Pressure (CoP) จากค่าแรงกดของเซนเซอร์แต่ละจุดในแต่ละเฟรม โดยใช้การถ่วงน้ำหนักตามตำแหน่งพิกัดของเซนเซอร์ แล้วแสดงผลเป็นจุดพิกัดพร้อมวาดเส้นทางการเคลื่อนที่ (Center of Pressure Trace) ลอยทับบนหน้า Heatmap โดยอัปเดตตำแหน่งอย่างต่อเนื่องตามความถี่ของข้อมูลที่รับเข้ามา เพื่อให้นักกีฬาเห็นการเคลื่อนที่และเส้นทางของจุดศูนย์แรงกดแบบเรียลไทม์
3.3.5.6 การจัดการสถานะและการบันทึกวิดีโอ (State Management and Data Sync)
หน้าจอ dashboard_screen.dart เปิดการรับฟังข้อมูลผ่าน StreamSubscription โดยใช้ _serialSubscription.listen(_processSerialData) ทันทีที่เซนเซอร์ส่งเฟรมข้อมูลเข้ามา ฟังก์ชันจะคำนวณค่าแรงกดและเรียก setState() เพื่อให้ Flutter Re-render เฉพาะ Widget ที่เปลี่ยนแปลง ได้แก่ CustomPainter สำหรับ Heatmap และ FlChart สำหรับกราฟสมดุลซ้าย-ขวา โดยไม่ Re-render ทั้งหน้าจอ ส่งผลให้การแสดงผลราบรื่นปราศจากความหน่วง
 
ภาพที่  3-34 แผนภาพการจัดการสถานะและการบันทึกวิดีโอ
ก)	การบันทึก Timeline Buffer แบบ Time-stamped เมื่อระบบเข้าสู่สถานะจับสวิง (isSwinging = true) ควบคู่กับการเริ่มบันทึกภาพวิดีโอกล้อง แอปพลิเคชันจะประกาศระบบอาเรย์แบบ List เป็นหน่วยความจำตัวกลาง (_recordedDataBuffer) เพื่อเก็บชุดโครงสร้างข้อมูลพร้อมเลขประทับเวลาอย่างต่อเนื่องตลอดสวิง

```dart
// ตัวอย่างชุดคำสั่งจัดเก็บข้อมูลไทม์ไลน์แรงกดระหว่างวงสวิง
void _recordCurrentFrameData() {
  if (!isSwinging) return;

  // อ่านเวลาของกรอบภาพนี้และจัดเก็บค่าที่ประมวลผลแล้ว
  int currentTimestamp = DateTime.now().millisecondsSinceEpoch;
  
  Map<String, dynamic> frameData = {
    'timestamp': currentTimestamp,
    'left_percentage': _leftForceRatio,
    'right_percentage': _rightForceRatio,
    'heatmap_matrix': _cachedHeatmapGrid,
    'cop_coordinate': {'x': _copX, 'y': _copY}
  };

  // นำก้อนข้อมูลระบุชนิด JSON แทรกลงในคิว
  _recordedDataBuffer.add(frameData);
}
```
ภาพที่  3-31 ภาพตัวอย่างชุดคำสั่งการจัดเก็บค่าดัชนีพร้อมเลขประทับเวลา
จากภาพที่ 3-31 แสดงโค้ดการทำงานเบื้องหลังการบันทึกเอกสารระบบ JSON Array ซึ่งกุญแจสำคัญคือการดึงค่าจากระบบนาฬิกาท้องถิ่นประทับเข้าไปในตัวแปร `currentTimestamp` ผูกติดอยู่กับค่าเปอร์เซ็นต์แรงกดและพิกัดทิ้งน้ำหนัก ก่อนนำส่งให้คลาวด์แปลงลงในคอลัมน์และพร้อมให้โหลดไปเล่นซ้ำ
ข)	การอัปโหลดและบันทึกข้อมูลลง Supabase เมื่อสิ้นสุดการสวิง เมื่อสิ้นสุดการสวิง ระบบจะหยุดการบันทึกวิดีโอผ่านคำสั่ง Stop แล้วอัปโหลดไฟล์ .mp4 ไปยัง Supabase Storage ผ่าน SensorDataService รับ Public Video URL กลับมา จากนั้นห่อ _recordedDataBuffer ทั้งหมดเป็น JSON แล้วบันทึกลงตาราง swings ในฐานข้อมูล Supabase พร้อมแนบ Video URL ที่ได้รับ ส่งผลให้ตอนหน้าจอเล่นซ้ำสามารถอ้างอิงข้อมูลแรงกดและวิดีโอถึงกันได้อย่างแม่นยำ
3.3.5.7 ระบบเล่นซ้ำและการซิงโครไนซ์ระดับมิลลิวินาที (High-Frequency Playback Synchronization)
โครงสร้างในหน้าจอวิเคราะห์ย้อนหลัง (PlaybackScreen) ถูกพัฒนาให้มีความสามารถในการซิงโครไนซ์วิดีโอกับข้อมูลแรงกดอย่างแม่นยำในระดับมิลลิวินาที โดยใช้ Timer.periodic ที่พัลส์ที่ระดับ 16 มิลลิวินาที ซึ่งเทียบเท่าอัตราการรีเฟรช 60 เฟรมต่อวินาที เพื่ออ่านตำแหน่งเวลาปัจจุบันของเครื่องเล่นวิดีโอ

```dart
// ตัวอย่างชุดคำสั่งการการทำงานของระบบนาฬิกาพัลส์สัมพันธ์เพลย์แบ็ก
void _startSynchronizationTimer() {
  _syncTimer = Timer.periodic(const Duration(milliseconds: 16), (timer) {
    if (_videoController!.value.isPlaying) {
      // ดึงวินาทีการเล่นปัจจุบันของวิดีโอ (แปลงเป็นมิลลิวินาที)
      int currentVideoMs = _videoController!.value.position.inMilliseconds;
      
      // ค้นหาเลข Index กรอบข้อมูลแรงกดที่เวลาแม่นยำหรือใกล้เคียงที่สุด
      int index = _findClosestTimelineIndex(currentVideoMs);
      
      if (index != _currentFrameIndex) {
        setState(() {
          _currentFrameIndex = index;
          _updateHeatmapAndChartsForFrame(index); // ส่งสัญญาณวาดจอใหม่
        });
      }
    }
  });
}
```
ภาพที่  3-32 ภาพตัวอย่างชุดคำสั่งควบคุมการเล่นซ้ำร่วมกับวิดีโอ (Synchronization Trigger)
จากภาพที่ 3-32 แสดงโค้ดการจัดการระบบคิวทำงานพัลส์ `Timer.periodic` บนความถี่สูง 60 Hertz โดยจุดประสงค์ของการจับเวลานี้ก็เพื่อเข้าไปถามตำแหน่งของคลิปวิดีโอปัจจุบันและนำมาเทียบเคียงหาช่วงข้อมูลแรงกดที่ถูกเก็บไว้ ถ้าเจอกันแอปพลิเคชันจะสั่งวาดกราฟิกให้ตรงกับจังหวะของกล้ามเนื้อที่ประคองไม้กอล์ฟทันที
ก)	การแมปเวลาวิดีโอกับข้อมูลแรงกด คำนวณหาสัดส่วนค่าสัมพัทธ์ (Proportion Index Mapping) จากตำแหน่งเวลาปัจจุบันของวิดีโอ แล้วนำไปดึงข้อมูลแรงกด ณ ช่วงเวลามิลลิวินาทีนั้น ๆ ออกมาจาก Timeline List แล้วเรียก setState() เพื่อ Re-render เฉพาะส่วน Heatmap และกราฟสมดุลให้ตรงกับวิดีโอ ทำให้จังหวะที่ไม้กอล์ฟกระแทกลูกกับสีของ Heatmap ที่พุ่งสูงขึ้นเกิดขึ้นพร้อมกันในทุกเสี้ยววินาทีอย่างแม่นยำ
ข)	ฟังก์ชัน Seek Scrubber และการเล่นช้า พัฒนาฟังก์ชันควบคุมการเล่นวิดีโอให้รองรับการหยุด (Pause) การเล่นช้า (Slow-motion) โดยปรับ Playback Speed ของ video_player และการเลื่อนดูทีละเฟรม (Seek Scrubber) เพื่อให้นักกีฬาและผู้ฝึกสอนสามารถวิเคราะห์รูปแบบการถ่ายเทน้ำหนักในแต่ละเสี้ยววินาทีได้อย่างละเอียด